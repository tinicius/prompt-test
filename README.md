export the open AI key. Using
export OPENAI_API_KEY="your_openai_api_key_here"

1. Set the env variable in a .env file:

   ```plaintext
   WEBHOOK_URL=http://localhost:5678/webhook/ai
   ```
# prompt-test
